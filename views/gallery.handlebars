
    <div class="container">
    <h2 class="center-align"> Cookie Gallery </h2>

    <hr>
    <br>
    <p class="center-align"> Take a peak at some of the beautiful cookies we have created!</p>
    </div>
    
    <br>

    <div class="container">
          {{#each cookies}}
        <div class="row"> 
             <div class="col s3 m3 l3" id="gallery"> 
             <img src="../img/{{this.image}}"  alt="{{this.name}}" class="circle responsive-img">
              <p id="galleryText"> {{this.name}}</p>
       
            </div>
  {{/each}}
</div>
</div>